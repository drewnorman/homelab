# Encrypt this file with: sops secrets/edge.yaml
#
# The cloudflare-dns-api-token value must be a valid systemd EnvironmentFile line
# so Caddy can read the token via {env.CLOUDFLARE_DNS_API_TOKEN}.
cloudflare-dns-api-token: "CLOUDFLARE_DNS_API_TOKEN=replace-with-cloudflare-api-token"
